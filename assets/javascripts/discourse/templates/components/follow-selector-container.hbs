{{#if showToggle}}
  {{follow-notifications-button
    user=user
    options=(hash
      icon=icon
      showFullTitle=showFullTitle
      preventsClickPropagation=true
      showCaret=showCaret
    )
  }}
{{/if}}
